sudo: false
language: ruby
cache: bundler

rvm: 
  - 2.2.8
  - 2.3.5
  - 2.4.2
  - ruby-head

script: TRAVIS_CI=1 bundle exec rake test

addons:
  postgresql: "9.4"

before_script:
  - psql -c 'create database pghero_test;' -U postgres

notifications:
  email:
    on_success: never
    on_failure: change
